# circulation/urls.py

from django.urls import path
from . import views  # views from this same app

app_name = 'circulation' 

urlpatterns = [
    # We'll add paths here step by step as we build each feature
]